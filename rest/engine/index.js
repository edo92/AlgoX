const dataset = require('./dataset');
const model = require('./model');
const predict = require('./predict');
const generate = require('./generate');

module.exports = { dataset, model, predict, generate };